import { Get, GetById, Delete, Update, Insert } from "./crud";
export const Database = {
  Get,
  GetById,
  Delete,
  Update,
  Insert,
};
import { createCheck } from "./check";
export const Check = {
  createCheck,
};
