import { Get, GetById, Delete, Update, Insert } from "./crud";
export const Database = {
  Get,
  GetById,
  Delete,
  Update,
  Insert,
};
import { Sf } from "./print";
export const Print = {
  Sf,
};
